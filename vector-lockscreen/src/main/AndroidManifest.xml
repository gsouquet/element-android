<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="im.vector.lockscreen">

    <uses-permission android:name="android.permission.VIBRATE" />
    <application>
        <!-- Used for UI tests to display the BiometricPrompt -->
        <activity android:exported="false" android:name=".tests.TestActivity" />
    </application>

</manifest>
